# Dependencies
node_modules
frontend/node_modules

# Environment
.env
.env.local
.env.*.local

# Hardhat
artifacts
cache
typechain-types

# Vite / frontend build
frontend/dist
frontend/.vite
frontend/coverage
coverage
coverage.json
.nyc_output
output/playwright
.playwright-cli

# Supabase local state
supabase/.branches
supabase/.temp

# NOTE: Legacy V1 dApp contract artifacts (frontend/src/contracts/*.json) are
# committed on purpose — the contract is frozen during the SaaS migration and the
# frontend build/tests import them. Regenerate via scripts/deploy.js if it changes.

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
